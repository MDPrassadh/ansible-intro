- name: Teardown VPC, Subnets, IGW, NAT, and SGs
  hosts: local
  connection: local
  collections:
    - amazon.aws

  vars:
    region: "ap-south-1"
    vpc_name: "prassadh"

  tasks:
    # Lookup VPC by Name tag
    - name: Get VPC info
      amazon.aws.ec2_vpc_net_info:
        filters:
          "tag:Name": "{{ vpc_name }}"
        region: "{{ region }}"
      register: vpc_info

    - name: Set VPC ID fact
      set_fact:
        vpc_id: "{{ vpc_info.vpcs[0].vpc_id }}"

    # Delete Security Groups
    - name: Delete SGs
      amazon.aws.ec2_group:
        name: "{{ item }}"
        region: "{{ region }}"
        vpc_id: "{{ vpc_id }}"
        state: absent
      loop:
        - prassadh-database
        - prassadh-backend
        - prassadh-frontend
        - prassadh-bastion

    # Delete Route Tables by tag name
    - name: Delete Route Tables
      amazon.aws.ec2_vpc_route_table:
        vpc_id: "{{ vpc_id }}"
        region: "{{ region }}"
        state: absent
        tags:
          Name: "{{ item }}"
      loop:
        - prassadh-db-rt
        - prassadh-private-rt
        - prassadh-public-rt

    # Delete NAT Gateways
    - name: Get NAT Gateway info
      amazon.aws.ec2_vpc_nat_gateway_info:
        filters:
          vpc-id: "{{ vpc_id }}"
        region: "{{ region }}"
      register: natgw_info

    - name: Delete NAT Gateways
      amazon.aws.ec2_vpc_nat_gateway:
        nat_gateway_id: "{{ item.nat_gateway_id }}"
        region: "{{ region }}"
        state: absent
        wait: true
      loop: "{{ natgw_info.result }}"

    # Delete Internet Gateway
    - name: Delete Internet Gateway
      amazon.aws.ec2_vpc_igw:
        vpc_id: "{{ vpc_id }}"
        region: "{{ region }}"
        state: absent

    # Delete Subnets by tag name
    - name: Delete Subnets
      amazon.aws.ec2_vpc_subnet:
        vpc_id: "{{ vpc_id }}"
        region: "{{ region }}"
        state: absent
        tags:
          Name: "{{ item }}"
      loop:
        - prassadh-public-subnet-1a
        - prassadh-public-subnet-1b
        - prassadh-private-subnet-1a
        - prassadh-private-subnet-1b
        - prassadh-database-subnet-1a
        - prassadh-database-subnet-1b

    # Finally delete VPC
    - name: Delete VPC
      amazon.aws.ec2_vpc_net:
        name: "{{ vpc_name }}"
        region: "{{ region }}"
        state: absent
